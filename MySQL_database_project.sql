show databases;
use enter_your_database_name;
    create table data (
    id int primary key auto_increment,
    name varchar(30),
    age int,
    address varchar(100),
    contact varchar(15),
    mail varchar(50)
    );
